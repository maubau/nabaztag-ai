# Secrets & personal data — never committed (see docs/ARCHITECTURE.md §11.3)
.env
config.yaml
intents.yaml
ojn/network/hostapd.conf
ojn/network/dnsmasq.conf

# Generated audio & model weights
www/audio/
*.mp3
# Weights stay out of git, but the PROVENANCE record must not: without it a
# .onnx in models/wake/ is an unidentifiable binary. Re-include just those.
models/*
!models/wake/
models/wake/*
!models/wake/*.provenance.md
*.bin
*.onnx
*.ct2

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
dist/
build/

# Local working copy of the spec; the canonical version is docs/ARCHITECTURE.md
/nabaztag-ai-architecture-v2.2.md

# Private working notes: the FULL annotated spec and findings log, with the
# measurements and the story behind each rule. Kept out of the public repo while
# the write-ups are being produced; docs/ holds the reference versions.
/notes/

# OS / editor
.DS_Store
.idea/
.vscode/
